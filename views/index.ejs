<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Blogger - Home</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- Google Fonts for modern look -->
    <link href="https://fonts.googleapis.com/css?family=Montserrat:700,400&display=swap" rel="stylesheet">
    <style>
        body {
            margin: 0;
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(120deg, #f8fafc 0%, #e0e7ff 100%);
            color: #22223b;
        }
        header {
            background: #4f46e5;
            color: #fff;
            padding: 2rem 0 1.5rem 0;
            text-align: center;
            box-shadow: 0 2px 8px rgba(79,70,229,0.08);
        }
        header h1 {
            margin: 0;
            font-size: 2.8rem;
            letter-spacing: 2px;
        }
        header p {
            margin: 0.5rem 0 0 0;
            font-size: 1.2rem;
            font-weight: 400;
        }
        .container {
            max-width: 900px;
            margin: 2.5rem auto;
            padding: 0 1.5rem;
        }
        .cta {
            display: flex;
            justify-content: center;
            gap: 1rem;
            margin: 2rem 0;
        }
        .cta a {
            background: #6366f1;
            color: #fff;
            padding: 0.8rem 2rem;
            border-radius: 30px;
            text-decoration: none;
            font-weight: 700;
            transition: background 0.2s;
            box-shadow: 0 2px 8px rgba(99,102,241,0.08);
        }
        .cta a:hover {
            background: #4338ca;
        }
        .blog-list {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(270px, 1fr));
            gap: 2rem;
        }
        .blog-card {
            background: #fff;
            border-radius: 18px;
            box-shadow: 0 4px 16px rgba(79,70,229,0.07);
            padding: 1.5rem;
            transition: transform 0.15s;
        }
        .blog-card:hover {
            transform: translateY(-6px) scale(1.03);
        }
        .blog-card h2 {
            margin: 0 0 0.5rem 0;
            font-size: 1.3rem;
            color: #4f46e5;
        }
        .blog-card p {
            margin: 0 0 1rem 0;
            color: #444;
        }
        .blog-card .meta {
            font-size: 0.9rem;
            color: #888;
        }
        @media (max-width: 600px) {
            header h1 {
                font-size: 2rem;
            }
            .container {
                padding: 0 0.5rem;
            }
        }
    </style>
</head>
<body>
    <header>
        <h1>Blogger</h1>
        <p>Your place to share stories, ideas, and inspiration</p>
    </header>
    <div class="container">
        <div class="cta">
            <a href="/create">Write a new Blog</a>
            <a href="/all">View all Blogs</a>
            <a href="/update">Update a blog</a>
            <a href="/about">About</a>
        </div>
        <section>
            <h2 style="margin-bottom:1.5rem; color:#22223b;">Latest Posts</h2>
            <div class="blog-list">
                <% if (locals.posts && locals.posts.length > 0) { %>
                    <% posts.forEach(function(post) { %>
                        <div class="blog-card">
                            <h2><a href="/posts/<%= post.title %>" style="color:inherit;text-decoration:none;"><%= post.title %></a></h2>
                            <p><%= post.author %>...</p>
                            <div class="meta">By <%= post.content || "Anonymous" %> &middot;</div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <p style="color:#888;">No posts yet. Be the first to <a href="/compose" style="color:#4f46e5;">write one</a>!</p>
                <% } %>
            </div>
        </section>
    </div>
</body>
</html>